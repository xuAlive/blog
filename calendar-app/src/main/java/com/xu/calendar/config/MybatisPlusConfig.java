package com.xu.calendar.config;

import org.springframework.context.annotation.Configuration;

/**
 * MyBatis-Plus 配置
 * MyBatis-Plus 3.5.x 默认已支持分页，无需额外配置
 */
@Configuration
public class MybatisPlusConfig {
    // 暂时无需配置，使用默认配置即可
    // 如需自定义分页，可添加 MybatisPlusInterceptor
}
